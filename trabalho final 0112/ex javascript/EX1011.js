function calcularSalario() (

let salarioAtual = document.getElementById('salarioAtual');
let tempoServico = document.getElementById('tempoServico');
let nome = document.getElementById('nome'). value;);


if (salarioAtual < 500 ) {
    reajuste = 0.25;
}